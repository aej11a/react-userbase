/*import {useIsSignedIn} from "react-userbase";
import React from "react";

export const AuthStatusIndicator = () => {
    const isSignedIn = useIsSignedIn()

    return (
        <div style={{margin: "auto", height: "200px", width: "200px", backgroundColor: isSignedIn ? "green" : "red"}}/>
    )
}
*/
